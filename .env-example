PORT=3000
EMAIL_USER=your-email@exemplo.com
EMAIL_PASS=your-password-smtp
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_SECURE=false

# Rate limit (optional): max requests per window; window in ms
RATE_LIMIT_MAX=10
RATE_LIMIT_WINDOW_MS=60000
